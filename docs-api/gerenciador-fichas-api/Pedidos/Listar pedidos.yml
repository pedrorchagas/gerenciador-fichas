info:
  name: Listar pedidos
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/pedidos"
  auth:
    type: bearer
    token: "{{token}}"
  params:
    - name: status
      value: paid
      type: query
      disabled: true

settings:
  encodeUrl: true
