info:
  name: Listar itens
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/itens"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: https://jargon-shone-manmade.ngrok-free.dev

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
